name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'projects/wizard-of-oz/package-lock.json'

    - name: Install dependencies
      run: |
        cd projects/wizard-of-oz
        npm ci

    - name: Build project
      run: |
        cd projects/wizard-of-oz
        npm run build

    # Vercel deployment (uncomment and configure if using Vercel)
    # - name: Deploy to Vercel
    #   id: deploy
    #   uses: amondnet/vercel-action@v25
    #   with:
    #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
    #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
    #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
    #     working-directory: ./projects/wizard-of-oz
    #     vercel-args: '--prod'

    # Netlify deployment (uncomment and configure if using Netlify)
    # - name: Deploy to Netlify
    #   id: deploy
    #   uses: nwtgck/actions-netlify@v3.0
    #   with:
    #     publish-dir: './projects/wizard-of-oz/dist'
    #     production-branch: main
    #     github-token: ${{ secrets.GITHUB_TOKEN }}
    #     deploy-message: "Deploy from GitHub Actions"
    #   env:
    #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
    #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

    # GitHub Pages deployment (uncomment if using GitHub Pages)
    # - name: Deploy to GitHub Pages
    #   id: deploy
    #   uses: peaceiris/actions-gh-pages@v3
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     publish_dir: ./projects/wizard-of-oz/dist

    # Generic deployment step - customize for your hosting provider
    - name: Deploy application
      id: deploy
      run: |
        echo "Add your deployment commands here"
        echo "Examples:"
        echo "- rsync to server"
        echo "- docker build and push"
        echo "- AWS S3 sync"
        echo "- Firebase deploy"
        echo "url=https://your-deployed-app.com" >> $GITHUB_OUTPUT

    - name: Deployment summary
      run: |
        echo "### 🚀 Deployment Complete" >> $GITHUB_STEP_SUMMARY
        echo "Successfully deployed to production!" >> $GITHUB_STEP_SUMMARY
        echo "URL: ${{ steps.deploy.outputs.url }}" >> $GITHUB_STEP_SUMMARY