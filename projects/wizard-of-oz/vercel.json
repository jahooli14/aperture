{
  "buildCommand": "npm run build",
  "ignoreCommand": "if [[ $VERCEL_GIT_COMMIT_AUTHOR_LOGIN == dependabot* ]]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- projects/wizard-of-oz || exit 1",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "memory": 2048
    }
  }
}
