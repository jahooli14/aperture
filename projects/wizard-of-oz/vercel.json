{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "memory": 2048
    }
  },
  "crons": [
    {
      "path": "/api/cron/send-reminders",
      "schedule": "0 15 * * *"
    }
  ],
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]]; then exit 0; fi; ROOT=$(git rev-parse --show-toplevel); BASE=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\"; git rev-parse \"$BASE\" >/dev/null 2>&1 || exit 1; git diff --quiet \"$BASE\" HEAD -- \"$ROOT/projects/wizard-of-oz\" || exit 1"
}