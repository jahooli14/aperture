{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "memory": 2048
    }
  },
  "buildCommand": "npm run build",
  "ignoreCommand": "bash -c '[ \"$VERCEL_ENV\" == \"production\" ] && git diff --quiet HEAD^ HEAD -- ./projects/wizard-of-oz/ && exit 0 || exit 1'"
}
