{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "memory": 2048
    }
  },
  "buildCommand": "npm install --arch=x64 --platform=linux sharp && pip3 install -r requirements.txt && npm run build"
}
