{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "memory": 2048
    }
  },
  "buildCommand": "npm install --arch=x64 --platform=linux sharp && python3 -m pip install -r requirements.txt && npm run build"
}
